<html>
  <head>
  </head>
  <body>
    <%= @debug %>

    <div id="pictures" class="text-center container">
      <% @pics.each do |thumbnail, compressed| %>
      <%= link_to image_tag(thumbnail), compressed %>
      <% end %>
    </div>
  </body>
</html>
